extends State

func enter(_msg: Dictionary = {}) -> void:
	pass
	## TODO: hook into your ammo component
	#await get_tree().create_timer(1.0).timeout  # TODO: real reload time
	#transitioned.emit(self, "OutOfCombat")
